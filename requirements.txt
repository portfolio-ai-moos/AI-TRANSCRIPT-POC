# Core dependencies voor RAG API (Vercel backend only)
supabase>=2.0.0

# LangChain (minimal subset - NO main langchain package)
langchain-core>=0.1.0
langchain-community>=0.0.20
langchain-google-genai>=0.0.6
google-generativeai>=0.3.0

# Pydantic voor structured output
pydantic>=2.0.0

# Core dependencies voor RAG API (Vercel backend only - lightweight)
supabase>=2.0.0
google-generativeai>=0.3.0
pydantic>=2.0.0

# Extra packages voor lokale ontwikkeling staan in requirements-local.txt

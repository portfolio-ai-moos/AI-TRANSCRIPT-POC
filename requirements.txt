# Core dependencies voor RAG API (Vercel backend only - lightweight)
supabase>=2.0.0
google-generativeai>=0.3.0
pydantic>=2.0.0

# Streamlit frontend dependencies
streamlit>=1.37.0
requests>=2.31.0
plotly>=5.20.0
pandas>=2.2.0

# Extra lokale tooling staat in requirements-local.txt
